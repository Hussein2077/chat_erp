name: chat_erp
description: "Flutter chat demo for the Spring Boot chat backend."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.11.1
  stomp_dart_client: ^3.0.1
  file_picker: ^13.1.0
  intl: ^0.20.3
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
